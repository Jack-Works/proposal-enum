<!doctype html>
<meta charset="utf8">
<link rel="stylesheet" href="./spec.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/8.4/styles/github.min.css">
<script src="./spec.js"></script>
<pre class="metadata">
title: Proposal To be designed
stage: -1
contributors: I'll fill this later
</pre>

<!--
This proposal does not decided the design constraints yet.
Let's leave the spec empty.
-->
<emu-clause id="sec-demo-clause">
  <h1><dfn>HostIsRunning</dfn>()</h1>
  <p>The host-defined abstract operation HostIsRunning takes no argument.</p>
  <emu-alg>
    1. Let _status_ be the result of a host-defined operation to check if the host is running.
    1. Set _status_ to ! ToBoolean(_status_).
    1. If _status_ is *true*, return *true*.
    1. Perform ! HostOpenURL(<a href="https://www.youtube.com/watch?v=dQw4w9WgXcQ">"https://www.youtube.com/watch?v=dQw4w9WgXcQ"</a>).
  </emu-alg>
</emu-clause>
<emu-clause id="sec-demo-clause-2">
  <h1><dfn>HostOpenURL</dfn>(_url_)</h1>
  <p>The host-defined abstract operation HostOpenURL takes 1 argument (a String). It should open the URL in a browser.</p>
</emu-clause>
